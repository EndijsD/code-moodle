import { Typography, styled } from '@mui/material';

export const Title = styled(Typography)({
  fontWeight: 'bold',
  background: 'linear-gradient(45deg, orange, orangered)',
  borderRadius: '100px / 110px 10px',
  width: 'fit-content',
  padding: '20px 30px',
  color: 'white',
  alignSelf: 'center',
  marginBottom: 32,
});
